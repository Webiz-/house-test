extends layout

block content
  h1 Location

  each model in models
      div(class='row')
          div(class="col-md-12")

                a(href='/house/' + model.name)
                    button(class='btn btn-primary')
                        span(class='badge badge-primary')= model.count
                        span= ' ' + model.name


